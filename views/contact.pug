extends templates/default

block content
    if message.length > 0
        .alert.alert-success(role='alert')
            p= message
    // Heading
    h2.display-4.font-weight-bold.white-text.mb-2 Contact Us
    h4.contact Leave a message and we will get back to you as soon as we can
    // Divider
    hr.hr-light
    .container-fluid.ride
        form(action='/contact', method='post')
            .form-group
            label(for='name') Name:
            input.form-control(type='text', id='name', required='')
            .form-group
            label(for='email') Email:
            input.form-control(type='email', required='')
            .form-group
            label(for='comment') Comment:
            textarea.form-control(type='text', name='comment', required='')
            br
            button.btn.btn-default(type='submit') Submit
    // /.Mask
    // /.Navbar
    // Main layout
block content1
    br
    .container-fluid.bg-grey
        .row
            .col-sm-1
            .col-sm-4
                h2 Contact Info
                br
                h5
                    span.glyphicon.glyphicon-briefcase
                    strong Company: 
                    |  Ride Enterprise
                h5
                    span.glyphicon.glyphicon-map-marker
                    strong Location:
                    |  4 Ulitsa Svoboda Bachvarova,    
                    |  Blagoevgrad, Bulgaria
                h5
                    span.glyphicon.glyphicon-phone
                    strong Telephone: 
                    |  +00 1515151515
                h5
                    span.glyphicon.glyphicon-envelope
                    strong email:
                    |  dnv151@aubg.edu
            .col-sm-6
                iframe(width='700', height='250', frameborder='0', style='border:0', src='https://www.google.com/maps/embed/v1/place?q=place_id:ChIJ0X2pKf71qhQRmCBrraxYrME&key=AIzaSyDX39h2vwBrkeTz8ZXHje5z83nld2JUvgA', allowfullscreen='')
            .col-sm-1

    // Main layout
    // Footer
    footer
    // Footer     